type Query {
  users: [User]
  user: User
}

type Mutation {
  user(email: String, name: String): User
  signIn(id: Int!): String
  signVerify(token: String!): String
}

type User {
  id: Int
  email: String
  name: String!
}
